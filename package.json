{
  "name": "gatsby-storybook-config",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "files": [
    "index.js"
  ],
  "scripts": {
    "build": "babel src --out-dir .",
    "prepare": "cross-env NODE_ENV=production npm run build"
  },
  "peerDependencies": {
    "@storybook/addon-actions": "^4.0.0",
    "@storybook/react": "^4.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.3.3",
    "babel-preset-gatsby-package": "^0.1.3",
    "cross-env": "^5.2.0"
  }
}
